# Environmental Platform - Local Development Environment
# Local development configuration for localhost

# Database Configuration (Local)
MYSQL_ROOT_PASSWORD=root123
MYSQL_PASSWORD=wordpress123

# Grafana Configuration (Local)
GRAFANA_PASSWORD=admin123

# Application Configuration (Development)
WORDPRESS_ENV=development
WORDPRESS_DEBUG=true

# SSL Configuration (Disabled for local)
SSL_ENABLED=false
DOMAIN_NAME=localhost

# Backup Configuration (Local)
BACKUP_RETENTION_DAYS=7
BACKUP_LOCATION=./backups

# Monitoring Configuration (Reduced for local)
PROMETHEUS_RETENTION=7d
GRAFANA_PLUGINS=grafana-piechart-panel,grafana-worldmap-panel

# Security Configuration (Disabled for local development)
FAIL2BAN_ENABLED=false
WAF_ENABLED=false

# Performance Configuration (Reduced for local)
REDIS_MAXMEMORY=128mb
MYSQL_INNODB_BUFFER_POOL_SIZE=256M

# Email Configuration (Optional for local - can be left empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Alert Configuration (Local)
ALERT_EMAIL=localhost@example.com
SLACK_WEBHOOK_URL=
